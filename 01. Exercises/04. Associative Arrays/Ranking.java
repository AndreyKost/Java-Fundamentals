import java.util.*;

public class Ranking {
    public static void main(String[] args) {
        Scanner scanner=new Scanner(System.in);
       /* String line =scanner.nextLine();
        Map<String, String> contests=new LinkedHashMap<>();
        Map<String, List<String>> contestUsers=new LinkedHashMap<>();
        Map<String, List<Integer>> userPoints=new LinkedHashMap<>();

        while (!line.equals("end of contests")){
            String[] tokens=line.split(":");
            String contest=tokens[0];
            String password=tokens[1];
            contests.put(contest,password);
            line=scanner.nextLine();
        }
        String input=scanner.nextLine();

        while (!input.equals("end of submissions")){
            String[] tokens=input.split("=>");
            String contest=tokens[0];
            String password=tokens[1];
            String user=tokens[2];
            int points=Integer.parseInt(tokens[3]);

            if(contests.containsKey(contest) && contests.get(contest).equals(password)){
                if(!contestUsers.containsKey(contest)){
                    contestUsers.put(contest,new ArrayList<>());
                    contestUsers.get(contest).add(user);
                }
                else {
                    if(!contestUsers.get(contest).contains(user)){
                        contestUsers.get(contest).add(user);
                    }
                }
                if(userPoints.containsKey(user)){
                    if(points > userPoints.get(user).get())
                } else {
                    userPoints.put(user, new ArrayList<>());
                    userPoints.get(user).add(points);
                }
            }
            input=scanner.nextLine();
        }
*/




    }
}
